# =============================================
# LAZY LLAMA v3.6 - Dependencies
# =============================================
# PyPI is the default index.
# Use extra index for CPU-only PyTorch.
# For GPU, override with:
#   pip install -r requirements.txt -i https://download.pytorch.org/whl/cu118
--extra-index-url https://download.pytorch.org/whl/cpu

# Core dependencies – use loose version constraints for compatibility
torch>=2.0.0
torchvision>=0.15.0
torchaudio>=2.0.0
transformers>=4.35.0
accelerate>=0.25.0
peft>=0.7.0
flask>=2.3.0
flask-cors>=4.0.0
huggingface-hub>=0.22.0
psutil>=5.9.0
rich>=13.7.0
textual>=0.41.0
markdown>=3.5.0
requests>=2.31.0
aiohttp>=3.9.0
tqdm>=4.66.0

# NumPy – allow 1.26.x as fallback; prefer 2.0+ but keep compatibility
numpy>=1.26.0,<3.0.0

# Tokenization and serialization
sentencepiece>=0.2.0
protobuf>=3.20.0
tokenizers>=0.19.0
safetensors>=0.3.0

# NLP and utilities
nltk>=3.8.1
packaging>=24.0

# HEPA support – required for CSV data loading and evaluation
pandas>=2.0.0
scikit-learn>=1.5.0

# Optional but recommended – keep as is
ollama-python>=0.1.0
llama-cpp-python>=0.2.27